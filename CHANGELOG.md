# 更新日志

格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## 未发布

### 新增

- **Redis 卡片多一行「库数」。** `INFO` 的 keyspace 段本来就逐库列着键数，此前只把它们
  相加成一个总数。一台「16 个库、键全在 db0」的实例与一台「键摊在 5 个库里」的实例，
  键总数可能一样，而后者上一句 `FLUSHDB` 只清掉五分之一 —— 面板上现在区分得出。
  空库不计入，故它答的是「有几个库在用」而非 `databases` 配置项；一个都没有时整行不出现
  （那时「0 个库」说不清是没在用还是探不到，而键数那一行已经答了）。

### 修复

- **虚拟化环境下显卡的标称显存不再丢失。** nvidia-smi 在那类机器上常把显存报成 `[N/A]`，
  而 `si.graphics()` 的 `vram` 还在。此前两路合并时只按 `looksNvidia` 把型号表里的 N 卡
  整条丢弃（免得同一块卡出现两次），那个数便跟着一起丢了 —— 表现为显卡卡片上只有占用率、
  没有「x GB / y GB」。改为先按名字包含式配对（新增 `namesMatch`，与 yenai-state 同一份
  判据），配上的把标称显存补给测到的那条；配不上时仍走原来的丢弃兜底，故同卡两现这件事
  没有回来。

### 变更

- 收掉 `probe.ts` / `net.ts` / `redis.ts` / `index.ts` 四处文件头的重复论证（同一条判据
  正反两面各说一遍的段落），注释占比 36% → 34%。判据本身一条没删：整机 CPU 首次采样
  不给 0、显卡两路合并为何各 spawn 一次 nvidia-smi、网卡按累计量而非速率筛、Redis
  为何不引客户端库，这些删了下一个人会改坏，故保留。代码一行未动。

## 0.2.0 — 2026-08-30

### 新增

- **「系统」组件**：CPU 与内存合成一张卡。左列是事实表（型号、核数、主频、占用、总量、已用），
  右侧两枚环各带「本进程 / 其他占用 / 空闲」三段图例。三段比例由 `src/rings.ts` 的 `ringLegend`
  算出、由用例钉住：其中「其他占用」是减出来的，故整机占用探不到时它给破折号而不是 0% ——
  0% 会被读成「机器闲着」。
- **本包自带 `style.css`**（`webuiPanel.style`）：`.legend-*`、`.sys-*` 与 `.ring-own` 这些类名在
  面板样式表里没有。面板会把其中每条选择器限定到本包之后才注入。
- **CI 工作流**。本仓库此前一个工作流都没有，而它是四个官方插件里唯一需要编译却没有门禁的：
  `src/rings.ts` 的几何算错不报错，只表现为「环画得不对」，那正是 154 个用例在钉的东西，却从没
  在 CI 上跑过。形制取 adapter-napcat 那份，多一段说明为何 `dist/` 不进仓库而 CI 必须自己 build。

### 变更

- 原 CPU、内存两枚改为 `defaultHidden`，否则同一份采样会在概览页出现两次。
- **peer 的 `^0.1.0` 改为 `>=0.1.0`。** 前者在 semver 里排除 0.2.0 —— 本包在内核 0.2.0 与 0.3.0
  上都装不上，而它用的 `probeGpus` 自初始提交就有，压根不需要那道上限。与 renderer-puppeteer
  早先修的是同一处。
- 索引的 `minWebui` 抬到 0.2.0：本包的 `dist/` 不进仓库，要靠面板商店代跑 `build` 才有可运行的
  产物，而那是 webui 0.2.0 才有的能力。装在更早的面板上会得到一个 node 侧加载失败的包。

### 修复

- **双色环一直是单色的。** `widgets/lib/store.js` 始终在环上输出 `.ring-own`，而面板样式表从未
  定义过这个类名 —— 两段首尾相接处看不出分界，且不报任何错。而那条分界正是「整机占用里有多少
  是机器人」的唯一可见凭据。本包自带的 `style.css` 补上了这一段。
- **README 的安装节写错了地方。** 本包是面板插件，装在**面板商店**、落点是
  `plugins/webui/plugins/`，而原文写的是插件市场与主目录的 `plugins/` —— 照着做会把包放进内核的
  插件目录，内核扫到一个没有 `yunzai` 段的目录并跳过，表现为「装了没反应」。

## 0.1.0 — 2026-08-29

整机 CPU、内存与各显卡占用，九枚面板组件加一份 node 侧采样，兼作面板插件包的完整范例。

环的几何写在 `src/rings.ts`（TypeScript）而不在 `widgets/`：两段弧的 dasharray 算错**不报错**，
只表现为「环画得不对」，故它由 vitest 钉住，浏览器侧 import 编译产物。这条路走得通的前提是
`src/rings.ts` 不 import 任何东西。
